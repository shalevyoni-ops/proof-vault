# proof-vault (public results)

Public transparency vault — **RESULTS ONLY**. Ed25519-signed scores, before/after, atom counts, hash matches, element distributions. Method lives only in the private vault.

## PERMANENT RULE — RESULTS vs METHOD (non-negotiable, permanent)

**RESULTS = public:** scores, before/after, atom counts, hash matches, element distributions, Ed25519 signatures.

**METHOD = PRIVATE ALWAYS:** neuron IDs, layer numbers, mechanisms, mri_layer, fix vectors, repair logic, fractal internals, classifier code.

Before ANY push to a public repo, audit every file for: `N731, N629, L63, L32, relu_residual, chain_skill, hybrid_chain, mri_layer, fixes.pkl, neuron, suppress, scale=0`. If ANY match -> it goes to `proof-vault-private`. No exceptions.
